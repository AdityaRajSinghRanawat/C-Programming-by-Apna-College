#include<stdio.h>

int main(){
    
    // int a = 8^8;      ===> valid     egzor = 8^8 gives 0
    // int x; int y = x; ===> valid     after ";" you can continue or shift to next line
    // int x, int y = x; ===> invalid   we are using x in same line(containing and declaring x)
    // int x, y = x;     ===> valid   
    // char stars = '**' ===> invalid   char = 1box, ** = 2box, not possible to store, same for '* '(space)
    
    return 0;
}