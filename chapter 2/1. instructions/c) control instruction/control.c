#include<stdio.h>

int main(){

    /*
        CONTOL INSTRUCTIONS:
        Used to determine flow of program

        a. Sequence Control || control sequence of code || everything which we learned till now
        b. Decision Control || if-else
        c. Loop Control     || for, while
        d. Case Control     || as per different case we perform different task
    */







    return 0;
}